export default function Head() {
  // Keep head minimal; analytics is loaded client-side in GaClient to
  // ensure the gtag runtime executes in the browser and captures SPA navigations.
  return null;
}
